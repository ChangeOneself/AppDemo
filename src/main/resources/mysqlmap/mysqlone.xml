<?xml version="1.0" encoding="UTF-8" ?>
        <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="app.mapper.MySqlMapper">
<update id="creatDatabase" parameterType="java.lang.String">
        create database if not exists `${databaseName}`;
    </update>

<select id="selectOne" resultType="java.lang.String">
        select id from one;
    </select>

    <update id="createAppTable">
        create table if not exists appdb_table_2330  (
            id varchar(6000),
            name varchar(6000)
        )
</update>

<select id="insertDataForPeople">
insert into appdb_table_2330 (id,name) values ("1","jiang");
</select>

    <select id="updateDataForPeople">
update appdb_table_2330 set name = 'jwc1' where id = '1';
</select>

</mapper>